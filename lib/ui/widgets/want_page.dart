import 'package:flutter/material.dart';

class WantsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('WANTS PAGE'),
    );
  }
}
